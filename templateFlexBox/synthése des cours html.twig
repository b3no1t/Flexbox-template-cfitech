synthése des cours html

— Balises semantique
  nav, article, section, header, footer, aside, main, picture, … 
— template html5
— Balises (40mn)
  — <table> 
  — <div>
  — <ul> <ol> <li>
  — <a>
  — <img> 
  — <meta> dans le head
  — <TODO> formulaire basic
  
  — Figma: Zonning, Prototype => design system  (45mn)


____
  table titre en couleur
  
